Save your 3 app screenshots here with these exact filenames:

  canvas-boundaries.png   — the canvas with Subscription/RG/VNet/Subnets and VMs
  validation-summary.png  — the green "Ready for Deployment" validation report
  waf-score.png           — the WAF Score view with Reliability/Security pillars

These files are referenced by src/components/AppShowcase.jsx and src/components/LandingIllustrations.jsx.
The site will work without them (shows fallback SVG) but looks much better with the real screenshots.
